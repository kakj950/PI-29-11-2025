# Приложение реализовано как единый монолитный сервис, объединяющий в себе:

клиентскую часть (frontend),
серверную логику (backend),
доступ к данным (база данных).

## Основные компоненты
1. Frontend (клиентская часть)
Реализован с помощью HTML, CSS и JavaScript.
Отображает интерфейс: список задач, форма создания/редактирования, фильтры, поиск.
Использует AJAX-запросы к REST API бэкенда для взаимодействия с сервером.
Поддерживает:
отображение дедлайнов и напоминаний,
выбор приоритета (низкий/средний/высокий),
фильтрацию по категориям и статусу,
интерфейс администратора (скрыт для обычных пользователей).
2. Backend (серверная часть)
Реализован на Python с использованием фреймворка Flask.
Обеспечивает:
маршрутизацию HTTP-запросов,
аутентификацию пользователей (разделение ролей: user и admin),
бизнес-логику: управление задачами, фильтрация, поиск, отслеживание прогресса,
генерацию уведомлений (в простейшем виде — запись в лог или флаг в БД).
Все обработчики и модели находятся в одном кодовом репозитории (монолит).
3. Database (хранилище данных)
Используется SQLite — лёгкая встраиваемая СУБД.
Хранит данные в локальном файле (например, tasks.db).
Основные таблицы:
users(id, username, password_hash, role)
tasks(id, title, description, deadline, reminder_time, priority, category, user_id, completed_at)
audit_log(id, action, task_id, user_id, timestamp) — для отслеживания изменений (для админа)
### Cхема компонентов
+--------------------------------------------------+
|                Web Browser (Frontend)            |
|  HTML / CSS / JavaScript (UI + AJAX-запросы)     |
+-----------------------+--------------------------+
                        |
                        | HTTP / HTTPS
                        v
+--------------------------------------------------+
|                Flask Backend (Python)            |
|  - Маршрутизация запросов                        |
|  - Логика управления задачами                    |
|  - Уведомления (в простейшем виде — логи/флаги)  |
|  - Аутентификация (user / admin)                 |
+-----------------------+--------------------------+
                        |
                        | SQLite API
                        v
+--------------------------------------------------+
|                Database (SQLite)                 |
|  Таблицы:                                        |
|    - users (id, name, role)                      |
|    - tasks (id, title, desc, deadline,           |
|            priority, category, user_id,          |
|            completed_at, reminder_time)          |
+--------------------------------------------------+
#### 📡 Основные API Endpoints
1) Аутентификация
POST /api/login — вход пользователя (возвращает сессию или токен)
POST /api/logout — выход
2) Задачи (Tasks)
GET /api/tasks — получить список задач (с возможностью фильтрации по категории, приоритету, статусу, дедлайну)
POST /api/tasks — создать новую задачу
(поля: title, description, deadline, priority, category, reminder_time)
GET /api/tasks/<id> — получить детали задачи
PUT /api/tasks/<id> — обновить задачу
DELETE /api/tasks/<id> — удалить задачу
3) Поиск и фильтрация
GET /api/tasks/search?q=<query> — поиск задач по названию или описанию
GET /api/tasks?category=work&priority=high&status=pending — фильтрация через query-параметры
4) Пользователи (для администратора)
GET /api/users — список пользователей (только для admin)
GET /api/users/<user_id>/tasks — задачи конкретного пользователя (для отслеживания прогресса)
5) Уведомления (упрощённо)
Уведомления реализованы как флаги в БД или системные логи (в реальном проекте — email/push)
GET /api/notifications — получить список активных уведомлений (для админа)